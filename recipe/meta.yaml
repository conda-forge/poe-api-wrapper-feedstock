{% set name = "poe-api-wrapper" %}
{% set version = "1.3.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/poe-api-wrapper-{{ version }}.tar.gz
  sha256: 9af7fbb6dc69767f6eaa7c06a1f6764318292eb36c6e33d6bf9e0e4265afa5bf

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - httpx
    - websocket-client
    - requests-toolbelt
    - loguru

test:
  imports:
    - poe_api_wrapper
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/snowby666/poe-api-wrapper
  summary: A simple API wrapper for Poe.com using Httpx
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - pedrohgmacedo
